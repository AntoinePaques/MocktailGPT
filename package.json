{
  "name": "mocktailgpt-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/@mocktailgpt/ts",
    "example/vanScrapper"
  ],
  "scripts": {
    "clean": "npm -ws run clean",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint",
    "format": "npm -ws run format",
    "gen": "npm -ws --if-present run gen"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.2.0"
  }
}
