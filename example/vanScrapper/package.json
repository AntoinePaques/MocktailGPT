{
  "name": "van-scrapper-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@mocktailgpt/ts": "workspace:*",
    "@faker-js/faker": "^8.4.0",
    "msw": "^2.10.2"
  },
  "scripts": {
    "clean": "rimraf generated",
    "gen": "mocktail",
    "lint": "eslint src",
    "build": "tsc",
    "format": "prettier -w \"**/*.ts\"",
    "generate": "mocktail generate --force",
    "test": "echo no tests"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "rimraf": "^5.0.0"
  }
}
